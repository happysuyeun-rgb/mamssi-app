# 작업 로그 — 2026-03-26

> 최종 업데이트: 2026-03-26

오늘 진행한 개발 구현/수정 사항 요약입니다.

## 주요 구현 항목
- 회원탈퇴(soft delete) 이후 재가입 시 복구 가능 기간(30일) 만료 처리
  - `AuthCallback`에서 `users.deleted_at` 기준으로 30일 초과이면 계정 재활성화 로직을 스킵하고 ` /login`으로 이동
  - 30일 이내인 경우 기존 동작대로 `is_deleted=false`, `deleted_at=null`로 재활성화 진행

## 검증
- `npm run type-check` 통과
- `npm run test:ci`(Vitest) 통과

